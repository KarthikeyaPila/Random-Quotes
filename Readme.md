# RandomQuotes

Tiny Java program that fetches a random fact from the Useless Facts API and prints it to the console.

## Run

```bash
javac RandomQuotes.java
java RandomQuotes
```

Requires **Java 11+** (uses `java.net.http.HttpClient`).
